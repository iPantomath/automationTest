<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="8dip"
    android:paddingRight="8dip"
    android:paddingBottom="8dip">
    <ImageView
        android:id="@+id/app_icon"
        android:layout_width="56dip"
        android:layout_height="56dip"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="4dip"/>
    <TextView
        android:id="@+id/release_type"
        style="@style/ReleaseTypeLabelStyle"
        android:layout_alignTop="@id/app_icon"
        android:layout_alignRight="@id/app_icon"
        app:fontFamily="@font/delivery_bold"/>
    <ImageView
        android:id="@+id/app_installed_marker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="4dip"
        app:srcCompat="@drawable/ic_app_installed_marker"/>
    <TextView
        android:id="@+id/app_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/app_icon"
        android:layout_toLeftOf="@id/app_installed_marker"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#333333"
        android:textSize="14sp"
        android:textStyle="bold"/>
    <TextView
        android:id="@+id/app_version"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/app_icon"
        android:layout_below="@id/app_name"
        android:textColor="#333333"
        android:textSize="13sp"/>
</RelativeLayout>